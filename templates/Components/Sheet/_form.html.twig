{{ form_start(form) }}
<div class="mdl-grid">
    <div class="mdl-cell mdl-cell--3-col"></div>
    <div class="mdl-cell mdl-cell--6-col d-flex flex-direction-column">
        <div>
            <h1>New Sheet()</h1>
        </div>
        <div class="mdl-textfield mdl-js-textfield mdl-textfield--floating-label max-width">
            {{ form_widget(form.title) }}
            {{ form_label(form.title) }}
        </div>
        <div class="mdl-textfield mdl-js-textfield max-width">
            {{ form_widget(form.description) }}
            {{ form_label(form.description) }}
        </div>
        <div>
            {{ form_label(form.content) }}
            {{ form_widget(form.content) }}
        </div>
            <div class="d-flex flex-direction-column mt-3">
                {{ form_label(form.language) }}
                {{ form_widget(form.language) }}
                <p>Vous ne trouvez pas votre langage ici ? crée le <a href="{{ path('language_new') }}">ici</a></p>
            </div>
        <div class="d-flex justify-content-center">
            <button class="mdl-button mdl-js-button mdl-button--raised mdl-button--colored">Persist</button>
        </div>
    </div>
</div>
{{ form_end(form) }}
